# Server-only URL used by Next.js route handlers. Tokens never reach browser storage.
BACKEND_URL=http://localhost:7575

# Optional preview mode. Never enable this in production.
NEXT_PUBLIC_DEMO_MODE=false
